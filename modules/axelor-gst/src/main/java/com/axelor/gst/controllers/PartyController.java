package com.axelor.gst.controllers;

import com.axelor.gst.db.Sequence;
import com.axelor.gst.db.repo.SequenceRepository;
import com.axelor.inject.Beans;
import com.axelor.meta.db.MetaModel;
import com.axelor.rpc.ActionRequest;
import com.axelor.rpc.ActionResponse;

public class PartyController {

	public void setReference(ActionRequest request, ActionResponse response)
	{
	/*	Sequence seq = request.getContext().asType(Sequence.class);
		seq = Beans.get(SequenceRepository.class)request.      
		
		MetaModel meta = new MetaModel();
		meta.getClass(Sequence.class);     */
		
	}
	
}
